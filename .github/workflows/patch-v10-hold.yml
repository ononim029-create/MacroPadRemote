name: Patch NEXO v1.0 hold
on:
  push:
    branches: [ feature/nexo-v10-workspace-controls ]
permissions:
  contents: write
jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[nexo-v10-hold]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feature/nexo-v10-workspace-controls
          fetch-depth: 0
      - name: Apply hold patch
        run: python tools/patch_v10_hold.py
      - name: Remove one-shot patch files
        run: rm -f tools/patch_v10_hold.py .github/workflows/patch-v10-hold.yml
      - name: Commit patched sources
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "NEXO v1.0: proper long-press hold lifecycle [nexo-v10-hold]"
          git push origin HEAD:feature/nexo-v10-workspace-controls
